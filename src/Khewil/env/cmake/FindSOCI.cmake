
set(SOCI_NAMEVERSION "soci-3.2.3")
set(SOCI_ROOT "${KHEWIL_THIRDPARTIES_INSTALL_DIR}/${SOCI_NAMEVERSION}")

if (EXISTS "${SOCI_ROOT}")
	set(SOCI_FOUND ON)
	set(SOCI_INCLUDES "${SOCI_ROOT}/inc")
	set(SOCI_LIBRARIES "${SOCI_ROOT}/lib")
	set(SOCI_BINARIES "${SOCI_ROOT}/bin")
	set(SOCI_BINARIES "${SOCI_BINARIES}" PARENT_SCOPE)
else()
	set(SOCI_FOUND OFF)
endif()

message(STATUS "SOCI_FOUND=${SOCI_FOUND}")
message(STATUS "SOCI_ROOT=${SOCI_ROOT}")
message(STATUS "SOCI_INCLUDES=${SOCI_INCLUDES}")
message(STATUS "SOCI_LIBRARIES=${SOCI_LIBRARIES}")
message(STATUS "SOCI_BINARIES=${SOCI_BINARIES}")
