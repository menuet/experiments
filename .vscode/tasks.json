{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "inputs": [
        {
            "id": "pkg_mgr",
            "type": "pickString",
            "description": "Package Manager to use",
            "options": [
                "vcpkg",
                "conan"
            ],
            "default": "conan"
        },
        {
            "id": "build_type",
            "type": "pickString",
            "description": "Build type to use",
            "options": [
                "Debug",
                "Release"
            ],
            "default": "Debug"
        }
    ],
    "tasks": [
        {
            "label": "Git Clean All !!!",
            "command": "git clean -xfd && git submodule foreach --recursive git clean -xfd",
            "type": "shell",
            "problemMatcher": []
        },
        {
            "label": "Show Configuration",
            "type": "shell",
            "command": "python build.py --pkg_mgr ${input:pkg_mgr} --build_type ${input:build_type}",
            "problemMatcher": []
        },
        {
            "label": "Acquire Third-parties",
            "type": "shell",
            "command": "python build.py --pkg_mgr ${input:pkg_mgr} --build_type ${input:build_type} --acquire",
            "problemMatcher": []
        },
        {
            "label": "Build Project",
            "type": "shell",
            "command": "python build.py --pkg_mgr ${input:pkg_mgr} --build_type ${input:build_type} --build",
            "problemMatcher": []
        },
        {
            "label": "Test Project",
            "type": "shell",
            "command": "python build.py --pkg_mgr ${input:pkg_mgr} --build_type ${input:build_type} --test",
            "problemMatcher": []
        },
        {
            "label": "Everything",
            "type": "shell",
            "command": "python build.py --pkg_mgr ${input:pkg_mgr} --build_type ${input:build_type} --acquire --build --test",
            "problemMatcher": []
        }
    ]
}